<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>🎡 Spin & Win a Gift Card!</title>
  <style>
    body {
      font-family: Arial, sans-serif;
      background: #f7f7f7;
      display: flex;
      justify-content: center;
      align-items: center;
      height: 100vh;
      margin: 0;
    }
    .container {
      text-align: center;
      background: #fff;
      padding: 30px;
      border-radius: 12px;
      box-shadow: 0 0 20px rgba(0,0,0,0.1);
      width: 350px;
    }
    h1 {
      color: #ff4081;
    }
    #wheel {
      width: 250px;
      height: 250px;
      border-radius: 50%;
      border: 8px solid #333;
      background: conic-gradient(#ff4081 0% 25%, #4caf50 25% 50%, #2196f3 50% 75%, #ffc107 75% 100%);
      margin: 20px auto;
      position: relative;
      transform: rotate(0deg);
      transition: transform 4s ease-out;
    }
    .arrow {
      width: 0;
      height: 0;
      border-left: 20px solid transparent;
      border-right: 20px solid transparent;
      border-bottom: 30px solid #333;
      position: absolute;
      top: -30px;
      left: calc(50% - 20px);
    }
    button {
      background: #28a745;
      color: #fff;
      padding: 15px 30px;
      border: none;
      border-radius: 8px;
      font-size: 18px;
      cursor: pointer;
    }
    button:hover {
      background: #218838;
    }
    .result {
      margin-top: 20px;
      font-size: 18px;
      display: none;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>🎡 Spin & Win!</h1>
    <div id="wheel-container">
      <div id="wheel"><div class="arrow"></div></div>
    </div>
    <button id="spin-btn">Spin Now</button>
    <div class="result" id="result">
      🎉 Congratulations! You won a reward.<br>
      <button id="claim-btn">👉 Claim Your Prize</button>
    </div>
  </div>

  <script>
    const wheel = document.getElementById('wheel');
    const spinBtn = document.getElementById('spin-btn');
    const result = document.getElementById('result');
    const claimBtn = document.getElementById('claim-btn');

    spinBtn.addEventListener('click', () => {
      const deg = Math.floor(5000 + Math.random() * 5000);
      wheel.style.transform = `rotate(${deg}deg)`;
      spinBtn.disabled = true;
      setTimeout(() => {
        result.style.display = 'block';
      }, 4000);
    });

    claimBtn.addEventListener('click', () => {
      // افتح إعلان الـ Popunder / SmartLink
      window.open(<script type='text/javascript' src='//pl27296725.profitableratecpm.com/4d/71/48/4d7148b149ebe0da7352370bf9b3f9e6.js'></script>);
    });
  </script>
</body>
</html>
